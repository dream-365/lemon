{
  "connections": {

  },

  "schemas": {

  },

  "intputs": {
    "name": "sample data input",
    "object": "object name or sql text",
    "connectionId": "name to indentify connection",
    "schemaId": "name to indentify schema",
    "filter": "simple filter expression",
    "parameters": [
      {"name": "parameter1", "required": true},
      {"name": "parameter2", "required": true}
    ]
  },
  "outputs": {
    
  }
}